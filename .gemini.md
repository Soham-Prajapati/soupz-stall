# .gemini.md — Soupz Agents System Configuration & Reference

## 🧠 Session Behavior Rules (Read First — Every Session)

1. **Language**: Always respond in English only. Never switch languages.

2. **Task Continuity**: At the start of every session, read `tasks.md` 
   in the project root. If any task is marked `[ ]` under 
   "🔧 In Progress", resume that task first before doing anything else. 
   Tell the user: "Resuming incomplete task: [task name]" before starting.

3. **Output Logging**: After completing any task write TWO files
   in the current project root. Both files must contain the 
   COMPLETE raw output — nothing summarized, nothing cut off.
   
   FILE A: last-output.md (full plain text)
   
   FILE B: last-output.html using this template:
   <!DOCTYPE html>
   <html>
   <head>
   <meta charset="UTF-8">
   <meta http-equiv="refresh" content="5">
   <title>Last Output</title>
   <style>
     body{background:#111;color:#eee;font-family:monospace;
          padding:20px;max-width:900px;margin:0 auto}
     h1{color:#2FA7A0}
     .done{color:#4CAF50}.partial{color:#E86A33}.failed{color:#D64545}
     pre{background:#1a1a1a;padding:15px;border-radius:8px;
         overflow-x:auto;white-space:pre-wrap;word-wrap:break-word;
         max-height:none;height:auto}
     .copy{background:#2FA7A0;color:#111;border:none;
           padding:4px 10px;border-radius:4px;cursor:pointer;
           font-family:monospace;font-size:13px}
     .copy:hover{background:#238f89}
     .section{margin:20px 0;border-left:3px solid #2FA7A0;
              padding-left:15px}
     .ts{color:#666;font-size:12px}
     .meta{display:grid;grid-template-columns:1fr 1fr;
           gap:10px;margin:10px 0}
     .meta-item{background:#1a1a1a;padding:10px;border-radius:6px}
     .meta-label{color:#666;font-size:11px;margin-bottom:4px}
     .meta-value{color:#eee;font-size:14px;font-weight:bold}
   </style>
   </head>
   <body>
   <h1>🍜 Last Output</h1>
   <p class="ts">Updated: [TIMESTAMP] — Auto-refreshes every 5s</p>
   
   <div class="section">
     <h2>[TASK NAME]</h2>
     <p class="[done/partial/failed]">Status: [STATUS]</p>
   </div>

   <div class="meta">
     <div class="meta-item">
       <div class="meta-label">Files Changed</div>
       <div class="meta-value">[NUMBER]</div>
     </div>
     <div class="meta-item">
       <div class="meta-label">Duration</div>
       <div class="meta-value">[TIME TAKEN]</div>
     </div>
   </div>

   <div class="section">
     <h2>Files Modified</h2>
     <pre>[LIST EVERY FILE CHANGED WITH LINE NUMBERS]</pre>
   </div>

   <div class="section">
     <h2>Full Raw Output
       <button class="copy"
         onclick="navigator.clipboard.writeText(
         document.getElementById('raw').innerText)">
         Copy
       </button>
     </h2>
     <pre id="raw">[COMPLETE RAW OUTPUT - every line, 
nothing cut off, no summarizing]</pre>
   </div>

   <div class="section">
     <h2>Errors / Warnings</h2>
     <pre>[ANY ERRORS OR "NONE" IF CLEAN]</pre>
   </div>

   </body>
   </html>

   IMPORTANT: The pre tag must never be height-limited.
   All content must be visible without scrolling inside the box.
   The full output goes in — no truncation ever.

4. **Task Tracking**: Every project has a tasks.md with these states:
   - [ ] pending
   - [/] in progress  
   - [x] completed
   - [!] blocked
   After every task update tasks.md accordingly.

5. **Task Locking**: Before starting ANY task, mark it [/] FIRST.
   If a task shows [/] already, skip it — another agent has it.

6. **Never Assume**: Confirm before any irreversible deletion 
   or overwrite.

7. **Project Identity**: This project is called Soupz Stall. 
   There is no "BMAD", "bmad", or "B-MAD" anywhere in this project. 
   The orchestrator persona is called "soupz-orchestrator". 
   All personas use the `soupz-` prefix. Never reference BMAD.

> **Role:** Senior Software Engineer / Lead Architect
> **Context:** This is the primary system-level context for Gemini CLI when working on `soupz-agents`.

## 🏗️ System Architecture

`soupz-agents` is a local-first multi-agent orchestration platform designed for observability and efficiency.

### Core Components
- **CLI Runtime (`bin/soupz.js`, `src/`)**: The engine that handles routing, session management, and agent execution.
- **Remote Server (`packages/remote-server`)**: A Node.js/Express bridge that exposes CLI functionality over HTTP/WebSockets with PTY support.
- **Web Dashboard (`packages/dashboard`)**: A React-based UI for monitoring and interacting with agents.
- **Agent Registry**: A collection of specialist personas (`defaults/agents/`) layered on top of headless agents (Copilot, Gemini).
- **Orchestrator**: Handles semantic routing and multi-agent coordination (decomposition, parallel dispatch, synthesis).

## 🧩 Key Concepts

### 1. Hooks (Lifecycle Extension)
Hooks are extension points where logic is injected into the agent's workflow:
- **Pre-execution**: `ContextPantry.recall()` (memory injection), `OllamaPreprocessor` (prompt optimization).
- **Execution**: `AgentSpawner.on('output')` (real-time stream processing).
- **Post-execution**: `Grading.recordResult()` (quality assessment), `MemoryPool.store()` (persistence), `processDelegations()` (task chaining).

### 2. MCP (Model Context Protocol)
Located in `src/mcp/client.js`. It allows `soupz-agents` to connect to external MCP servers (like `npx @stitch/mcp-server`), enabling agents to use tools provided by those servers.
- **Register**: `/mcp register <name> <cmd>`
- **Connect**: `/mcp connect <name>`
- **Call**: `/mcp call <server> <tool> <args>`

### 3. Memory & Context
- **ContextPantry (`src/core/context-pantry.js`)**: Manages the immediate conversation context.
- **MemoryPool (`src/memory/`)**: Long-term storage for task results, enabling cross-session continuity.
- **TokenCompressor**: Shortens long prompts to save tokens while preserving meaning.

### 4. Plan Mode (Task Decomposition)
Implemented via the `todoList` in `src/session.js`.
- If a prompt is detected as "complex" (multi-step), it is automatically broken into a task list.
- Users can manage tasks with `/todo`, `/do 1`, and `/do all`.
- Orchestration uses `orchestrator.decompose()` to split a prompt into sub-tasks for parallel execution.

### 5. Skills
Defined in `src/skills.js`. Skills are discoverable specialist agents (personas) that can be invoked via `@name`. They are essentially pre-configured system prompts that wrap headless agents.

## 🚀 Efficiency Strategies (Inspired by Ruflo)

- **Smart Routing**: Route simple tasks to `qwen2.5:1.5b` (Ollama) or regex-based rules. Reserve premium models for complex reasoning.
- **Token Optimization**: Use `TokenCompressor` and only include relevant context from the `ContextPantry`.
- **Parallelism**: Use `orchestrateMultiAgent` or `/fleet` to run sub-tasks in parallel across different engines (Copilot and Gemini) to reduce total execution time.

## 📁 Directory Structure Summary
- `src/agents/`: Spawning logic.
- `src/orchestrator/`: Routing and decomposition.
- `src/core/`: Monitoring, compression, and pantry logic.
- `src/mcp/`: MCP client.
- `packages/dashboard/`: Frontend.
- `packages/remote-server/`: Backend bridge.
- `packages/browser-extension/`: Browser integration (Scaffold).
- `packages/mobile-ide/`: Mobile control (Scaffold).

## 🛠️ Development Guidelines
- **Staging/Committing**: Do not stage or commit unless explicitly asked.
- **Validation**: Always run `npm test` or specific verification scripts after changes.
- **Style**: Adhere to the "Stall/Kitchen" metaphor in user-facing components.
- **Security**: Protect `.env` and Supabase service-role keys.
