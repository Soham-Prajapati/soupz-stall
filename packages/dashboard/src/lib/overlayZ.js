// Shared overlay stacking convention for deterministic layering across mobile and desktop.
export const OVERLAY_Z = Object.freeze({
  setupWizard: 100,
  modal: 120,
  toast: 130,
  commandPalette: 140,
  folderPicker: 200,
});
